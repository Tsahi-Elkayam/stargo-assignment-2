global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: 'bitcoin-exporter'
    static_configs:
      # Use the public Railway URL of your bitcoin exporter
      # Replace with your actual URL from 'railway domain'
      - targets: ['stargo-assignment-2-production.up.railway.app']
    scrape_interval: 60s
    scrape_timeout: 10s
    scheme: https
