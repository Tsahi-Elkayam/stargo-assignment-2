# Prometheus for Render
FROM prom/prometheus:latest

# Add entrypoint script (ADD sets executable automatically)
ADD prometheus-start.sh /prometheus-start.sh

EXPOSE 9090

ENTRYPOINT ["/bin/sh", "/prometheus-start.sh"]
