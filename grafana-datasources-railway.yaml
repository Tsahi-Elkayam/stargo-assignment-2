apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Update this URL after deploying Prometheus
    # Use the public URL from 'railway domain'
    url: https://prometheus-production.up.railway.app
    isDefault: true
    editable: true
